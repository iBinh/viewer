<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:OpenIPC.Viewer.App.ViewModels.Dialogs"
             xmlns:svc="using:OpenIPC.Viewer.App.Services"
             x:Class="OpenIPC.Viewer.App.Views.Dialogs.DiscoveryDialogContent"
             x:DataType="vm:DiscoveryDialogViewModel"
             MaxWidth="560" MaxHeight="700"
             Background="{StaticResource Bg1Brush}"
             FontFamily="{StaticResource FontSans}"
             FontSize="{StaticResource FontSizeBase}"
             Foreground="{StaticResource TextPrimaryBrush}">

  <Grid RowDefinitions="Auto,Auto,Auto,*,Auto,Auto" Margin="24">

    <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="0,0,0,12">
      <StackPanel Grid.Column="0" Spacing="4">
        <TextBlock Text="{Binding [Discovery.Header], Source={x:Static svc:Localizer.Instance}}"
                   FontSize="{StaticResource FontSizeLg}"
                   FontWeight="SemiBold" />
        <TextBlock Text="{Binding StatusText}"
                   FontSize="{StaticResource FontSizeSm}"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   TextWrapping="Wrap" />
      </StackPanel>
      <StackPanel Grid.Column="1" Spacing="6" HorizontalAlignment="Right">
        <Button Content="{Binding [Discovery.Button.Scan], Source={x:Static svc:Localizer.Instance}}"
                Command="{Binding ScanCommand}"
                Padding="14,8"
                Background="{StaticResource AccentBrush}"
                Foreground="White"
                CornerRadius="6"
                HorizontalAlignment="Right" />
        <CheckBox IsChecked="{Binding DeepScan, Mode=TwoWay}"
                  IsEnabled="{Binding !ScanInProgress}"
                  Content="{Binding [Discovery.DeepScan], Source={x:Static svc:Localizer.Instance}}"
                  FontSize="{StaticResource FontSizeSm}"
                  Foreground="{StaticResource TextSecondaryBrush}" />
      </StackPanel>
    </Grid>

    <!-- Where a deep scan goes. The subnets come from the OS routing table and
         arrive ticked, so a camera on another VLAN or behind a VPN needs no
         typing at all; the box below is for the odd range routes don't show.
         Bad input or an over-wide selection shows at the bottom and disables Scan. -->
    <StackPanel Grid.Row="1" Spacing="6" Margin="0,0,0,12">
      <!-- The window is fixed-height, and a machine on several VLANs can turn up
           many subnets, so the list scrolls inside a capped box instead of
           pushing the rest of the dialog off the bottom. -->
      <ScrollViewer IsVisible="{Binding HasScanTargets}"
                    MaxHeight="132"
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding ScanTargets}">
          <ItemsControl.ItemTemplate>
            <DataTemplate DataType="vm:ScanTargetRowVm">
              <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                        IsEnabled="{Binding $parent[UserControl].((vm:DiscoveryDialogViewModel)DataContext).ScanInProgress, Converter={x:Static BoolConverters.Not}}"
                        Margin="0,0,0,2">
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="{Binding Cidr}"
                             FontFamily="{StaticResource FontMono}"
                             Foreground="{StaticResource TextPrimaryBrush}" />
                  <TextBlock Text="{Binding Detail}"
                             FontSize="{StaticResource FontSizeSm}"
                             VerticalAlignment="Center"
                             Foreground="{StaticResource TextTertiaryBrush}" />
                </StackPanel>
              </CheckBox>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>

      <TextBox Text="{Binding IpRangeText, Mode=TwoWay}"
               IsEnabled="{Binding !ScanInProgress}"
               FontFamily="{StaticResource FontMono}"
               PlaceholderText="{Binding [Discovery.IpRange.Placeholder], Source={x:Static svc:Localizer.Instance}}" />

      <TextBlock Text="{Binding SweepSummary}"
                 IsVisible="{Binding SweepSummary, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                 FontSize="{StaticResource FontSizeSm}"
                 Foreground="{StaticResource TextSecondaryBrush}" />
      <TextBlock Text="{Binding IpRangeError}"
                 IsVisible="{Binding IpRangeError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                 Foreground="{StaticResource DangerBrush}"
                 FontSize="{StaticResource FontSizeSm}"
                 TextWrapping="Wrap" />
    </StackPanel>

    <!-- Scan progress (0..1), shown only while scanning. -->
    <ProgressBar Grid.Row="2"
                 Minimum="0" Maximum="1"
                 Value="{Binding ScanProgress}"
                 IsVisible="{Binding ScanInProgress}"
                 Height="4"
                 Margin="0,0,0,12"
                 Foreground="{StaticResource AccentBrush}" />

    <Border Grid.Row="3"
            Background="{StaticResource Bg2Brush}"
            BorderBrush="{StaticResource BorderWeakBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="2"
            MinHeight="200">
      <ListBox ItemsSource="{Binding Cameras}"
               SelectedItem="{Binding Selected, Mode=TwoWay}"
               Background="Transparent"
               BorderThickness="0">
        <ListBox.ItemTemplate>
          <DataTemplate DataType="vm:DiscoveredDeviceRowVm">
            <Grid ColumnDefinitions="*,Auto" Margin="8">
              <StackPanel Grid.Column="0" Spacing="2">
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="{Binding DisplayName}"
                             FontWeight="Medium"
                             Foreground="{StaticResource TextPrimaryBrush}" />
                  <Border IsVisible="{Binding IsAlreadyAdded}"
                          Background="{StaticResource Bg3Brush}"
                          CornerRadius="4" Padding="6,1"
                          VerticalAlignment="Center">
                    <TextBlock Text="{Binding [Discovery.AlreadyAdded], Source={x:Static svc:Localizer.Instance}}"
                               FontSize="{StaticResource FontSizeSm}"
                               Foreground="{StaticResource SuccessBrush}" />
                  </Border>
                </StackPanel>
                <TextBlock Text="{Binding Subtitle}"
                           FontSize="{StaticResource FontSizeSm}"
                           Foreground="{StaticResource TextSecondaryBrush}" />
              </StackPanel>
              <StackPanel Grid.Column="1" Spacing="2" HorizontalAlignment="Right">
                <TextBlock Text="{Binding HostPort}"
                           FontFamily="{StaticResource FontMono}"
                           FontSize="{StaticResource FontSizeSm}"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Right" />
                <TextBlock FontSize="{StaticResource FontSizeSm}"
                           Foreground="{StaticResource TextTertiaryBrush}"
                           HorizontalAlignment="Right">
                  <Run Text="{Binding ProtocolsText}" />
                  <Run Text=" · " />
                  <Run Text="{Binding ConfidenceText}" />
                </TextBlock>
              </StackPanel>
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Border>

    <StackPanel Grid.Row="4" Spacing="8" Margin="0,16,0,0"
                IsVisible="{Binding IsRowSelected}">
      <Grid ColumnDefinitions="*,12,*">
        <StackPanel Grid.Column="0" Spacing="6">
          <TextBlock Text="{Binding [CameraEditor.Label.Username], Source={x:Static svc:Localizer.Instance}}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeSm}" />
          <TextBox Text="{Binding Username, Mode=TwoWay}" PlaceholderText="{Binding [CameraEditor.Placeholder.Username], Source={x:Static svc:Localizer.Instance}}" />
        </StackPanel>
        <StackPanel Grid.Column="2" Spacing="6">
          <TextBlock Text="{Binding [CameraEditor.Label.Password], Source={x:Static svc:Localizer.Instance}}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeSm}" />
          <TextBox Text="{Binding Password, Mode=TwoWay}" PasswordChar="•" />
        </StackPanel>
      </Grid>
      <CheckBox IsChecked="{Binding ReuseCredentials, Mode=TwoWay}"
                Content="{Binding [Discovery.ReuseCreds], Source={x:Static svc:Localizer.Instance}}"
                FontSize="{StaticResource FontSizeSm}"
                Foreground="{StaticResource TextSecondaryBrush}" />
    </StackPanel>

    <Grid Grid.Row="5" ColumnDefinitions="*,Auto,Auto" Margin="0,16,0,0">
      <Button Grid.Column="1"
              Name="CancelButton"
              Content="{Binding [Common.Cancel], Source={x:Static svc:Localizer.Instance}}"
              Padding="14,8"
              Background="Transparent"
              BorderBrush="{StaticResource BorderMediumBrush}"
              BorderThickness="1"
              CornerRadius="6"
              Foreground="{StaticResource TextPrimaryBrush}"
              Margin="0,0,8,0" />
      <Button Grid.Column="2"
              Name="AddButton"
              Content="{Binding [Discovery.Button.AddSelected], Source={x:Static svc:Localizer.Instance}}"
              IsEnabled="{Binding CanAdd}"
              Padding="14,8"
              Background="{StaticResource AccentBrush}"
              Foreground="White"
              CornerRadius="6"
              IsDefault="True" />
    </Grid>

  </Grid>

</UserControl>
